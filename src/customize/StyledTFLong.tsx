import { styled } from "@mui/system";
import { TextField } from "@mui/material";

const StyledTFLong = styled(TextField)(({ theme }) => ({
  width: "504px",
  flexShrink: 0,
}));

export default StyledTFLong;
