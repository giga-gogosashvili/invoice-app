import { styled } from "@mui/system";
import { TextField } from "@mui/material";

const StyledTFShort = styled(TextField)(() => ({
  width: "152px",
  flexShrink: 0,
}));

export default StyledTFShort;
