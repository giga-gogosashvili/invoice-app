import { StyledDarkModeIcon } from "src/customize/StyledIcons";

// interface Prop {
//   func?: any;
// }
export default function DarkButton() {
// {
//   func,
// }: Prop
  //   { func }: { func?: any } = {}
  // { func }: { func?: any } = {}
  //   const toggleColorMode: any = () => {
  //     setMode((prevMode: any) => (prevMode === "light" ? "dark" : "light"));
  //   };
  return <StyledDarkModeIcon />;
}
